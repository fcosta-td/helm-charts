apiVersion: v1
kind: Service
metadata:
  name: {{ include "dummy-automation-app.fullname" . }}
  labels:
    {{- include "dummy-automation-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dummy-automation-app.selectorLabels" . | nindent 4 }}
